if __name__ == "__main__":
    import uvicorn
    uvicorn.run("main:app", host="127.0.0.1", port=,reload=False,workers=1)
#left the port blank so it would error out if future me forgot :) 28.jan.2026
